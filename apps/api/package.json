{
  "name": "api",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node bin/build.js",
    "dev": "node bin/build.js dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "fastify": "^3.29.0"
  },
  "devDependencies": {
    "config": "workspace:*",
    "db": "workspace:*",
    "esbuild": "^0.14.39",
    "tsconfig": "workspace:*",
    "typescript": "^4.6.4"
  }
}
